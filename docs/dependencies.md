## Dependencies

Joaoaalves.DDD has **no external runtime dependencies**.

It relies exclusively on:

* .NET Standard / .NET runtime libraries

This makes it safe to use in:

* Domain layers
* Shared kernel projects
* Libraries consumed by multiple services 