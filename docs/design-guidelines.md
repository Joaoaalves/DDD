# Design Guidelines

* Keep domain logic inside the domain
* Favor explicit rules over implicit checks
* Use value objects aggressively
* Raise domain events only for meaningful business occurrences
* Treat aggregate roots as transactional boundaries
