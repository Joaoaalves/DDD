# Common Pitfalls

* Putting persistence logic inside entities
* Treating entities as anemic data models
* Using domain events as integration events
* Skipping aggregate boundaries

Joaoaalves.DDD encourages explicit modeling but does not prevent misuse.
